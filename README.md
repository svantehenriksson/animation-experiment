# Collection of early animation tryouts

Mostly ffmpeg created. Import a few frames like snapshots of a conversation and programmatically add them one after the other in a way that's synchronized with the audio track.

